package sec06.exam01.day88;

public class Car {
	//패키지는 클래스의 일부
	//패키지의 이름 규칙
	//숫자로 시작해서는 안 되고 _, $를 제외한 특수 문자를 사용해서는 안된다.
	//java로 시작하는 패키지는 자바 표준 API에서만 사용하므로 사용하면 안된다.
	//모두 소문자로 작성하는 것이 관례다
}
